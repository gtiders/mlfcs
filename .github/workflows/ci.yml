name: Documentation CI

on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
  group: docs-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docs:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
          python-version: "3.12"
          version: "0.11.26"
      - name: Install documentation environment
        run: uv sync --group docs
      - name: Verify README single source of truth
        run: uv run python scripts/sync_readmes.py --check
      - name: Verify bilingual documentation structure
        run: uv run python scripts/check_docs.py
      - name: Build documentation
        run: uv run --group docs mkdocs build --strict -f mkdocs.yml
      - name: Verify published layout
        run: |
          test -f site/index.html
          test -f site/en/index.html
          test -f site/zh/index.html
          test -f site/en/api/finite-difference-api/index.html
          test -f site/zh/api/finite-difference-api/index.html
          test -f site/en/api/fitting-api/index.html
          test -f site/zh/api/fitting-api/index.html
          test -f 'site/en/Q&A/index.html'
          test -f 'site/zh/Q&A/index.html'
